version: '3.8'

services:
  postgres:
    image: postgres:15.3-alpine
    volumes:
      - postgres:/var/lib/postgresql/data/
    ports:
      - 5440:5432
    env_file:
      - .env

volumes:
  postgres: {}